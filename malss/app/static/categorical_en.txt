Check the types of features (categorical or numerical) in the table below are correctly classified.<br>
<strong>Some features may have to be handled as the categorical feature even though they are looks like numerical features</strong> (e.g. Country names may be being replaced to country codes).<br>
If tha data has such features, <strong>change the checked state of the features from "numerical" to "categorical" in the table below</strong>.<br>
<br>
Finally, set the column of "target" of target variable checked.